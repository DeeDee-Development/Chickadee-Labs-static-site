---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Fall Prevention for Caregivers | DeeDee Health"
  description="Keep aging parents safe from falls with proactive health monitoring. Get early alerts when fall risks are detected. Peace of mind for family caregivers."
  simpleNav={true}
>
  <div class="caregivers-page">
    <!-- Simple Hero -->
    <section class="simple-hero">
      <div class="container">
        <h1>Keep Your Parents Safe from Falls</h1>
        <p class="lead">
          You're juggling enough already. DeeDee monitors fall risks so you don't have to worry—
          and alerts you before emergencies happen.
        </p>
        <a href="https://docs.google.com/forms/d/e/1FAIpQLSd8vG1putqxX1lLQr-uyecPMp6ztvFGA86BSm8XBlSxT8rJ7A" target="_blank" rel="noopener noreferrer" class="cta-button">Join Waitlist</a>
      </div>
    </section>

    <!-- Simple 3-Column -->
    <section class="simple-features">
      <div class="container">
        <div class="features-grid">
          <div class="feature">
            <h3>📱 Simple Setup</h3>
            <p>Just an app on their iPhone. No wearables, no cameras, no hassle.</p>
          </div>
          <div class="feature">
            <h3>🔔 Early Alerts</h3>
            <p>We detect fall risks before they happen. Not after.</p>
          </div>
          <div class="feature">
            <h3>😌 Peace of Mind</h3>
            <p>They stay independent. You stop worrying.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Simple CTA -->
    <section class="simple-cta">
      <div class="container">
        <h2>Ready to stop worrying?</h2>
        <a href="https://docs.google.com/forms/d/e/1FAIpQLSd8vG1putqxX1lLQr-uyecPMp6ztvFGA86BSm8XBlSxT8rJ7A" target="_blank" rel="noopener noreferrer" class="cta-button secondary">Get Started</a>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  .caregivers-page {
    font-family: var(--font-family);
  }

  /* Simple Hero - No gradient, clean white */
  .simple-hero {
    background: white;
    padding: 6rem 0 5rem;
    text-align: center;
    border-bottom: 1px solid var(--color-border);
  }

  .simple-hero h1 {
    font-size: 3rem;
    font-weight: var(--font-weight-bold);
    color: var(--color-text);
    margin-bottom: 2rem;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  .simple-hero .lead {
    font-size: 1.25rem;
    color: var(--color-text-light);
    line-height: 1.8;
    max-width: 700px;
    margin: 0 auto 3rem;
  }

  /* Simple CTA Button */
  .cta-button {
    display: inline-block;
    padding: 1rem 2.5rem;
    font-size: 1.125rem;
    font-weight: var(--font-weight-semibold);
    color: white;
    background: var(--color-text);
    border-radius: 8px;
    text-decoration: none;
    transition: all var(--transition-base);
  }

  .cta-button:hover {
    background: var(--color-primary);
    transform: translateY(-2px);
  }

  .cta-button.secondary {
    background: var(--color-primary);
  }

  .cta-button.secondary:hover {
    background: var(--color-accent);
  }

  /* Simple Features - Clean layout, no cards */
  .simple-features {
    padding: 6rem 0;
    background: var(--color-bg-subtle);
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 4rem;
    max-width: 1000px;
    margin: 0 auto;
  }

  .feature {
    text-align: center;
  }

  .feature h3 {
    font-size: 1.5rem;
    font-weight: var(--font-weight-semibold);
    color: var(--color-text);
    margin-bottom: 1.5rem;
  }

  .feature p {
    font-size: 1.125rem;
    color: var(--color-text-light);
    line-height: 1.7;
    margin: 0;
  }

  /* Simple CTA Section */
  .simple-cta {
    padding: 6rem 0;
    text-align: center;
    background: white;
  }

  .simple-cta h2 {
    font-size: 2rem;
    font-weight: var(--font-weight-bold);
    color: var(--color-text);
    margin-bottom: 2.5rem;
  }

  /* Mobile */
  @media (max-width: 768px) {
    .simple-hero {
      padding: 4rem 0 3rem;
    }

    .simple-hero h1 {
      font-size: 2rem;
    }

    .simple-hero .lead {
      font-size: 1.125rem;
    }

    .simple-features {
      padding: 4rem 0;
    }

    .features-grid {
      gap: 3rem;
    }

    .feature h3 {
      font-size: 1.25rem;
      margin-bottom: 1rem;
    }

    .feature p {
      font-size: 1rem;
    }

    .simple-cta {
      padding: 4rem 0;
    }

    .simple-cta h2 {
      margin-bottom: 2rem;
    }
  }
</style>
